security:
  basic:
    enabled: false
spring:
  datasource:
    platform: h2
logging:
  pattern:
    console: "%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%5p) %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n%wEx"
  level:
    root: info
    org:
      springframework:
        data:
          jpa: info
        test: warn
    nl:
      computerhok:
        cruddie: warn
